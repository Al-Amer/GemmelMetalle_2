<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gemmelmetalle">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GemmelMetalle">
        <activity android:name=".seveth_Activity2"></activity>
        <activity android:name=".FinalActivity" />
        <activity android:name=".SixthActivity" />
        <activity android:name=".FivethActivity" />
        <activity android:name=".FourthActivity" />
        <activity android:name=".TherthActivity" />
        <activity android:name=".SecandActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>